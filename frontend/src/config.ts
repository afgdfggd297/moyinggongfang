/** 前端全局配置 */

// API 超时（毫秒）
export const API_TIMEOUT = 30_000           // 默认请求超时
export const API_TIMEOUT_LONG = 180_000    // 长时间操作（生成/导出）
export const API_TIMEOUT_STREAM = 120_000  // 流式请求

// DOCX 预估目标内容长度（字符）
export const DOCX_TARGET_LENGTH = 5000
